import java.io.FileNotFoundException;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Random;

public class Temp {










/*
    for count 0, count < word length, count++
        if current coord is not " "
            add current char to getLetters
            intersect point at the current charAt
        switch(direction)
            if horizontal x++
            if vertical y++
            //if diagonal up x++ y--
            //if diagonal down x++ y++
            //if backward x--

        if intersects at 2+ points
            assign new coords to x1 & y1

        if intersects at 1 point
            get word where intersect # = intersect letter, then print it

        if intersects at 0 points
            print word

 */


}
